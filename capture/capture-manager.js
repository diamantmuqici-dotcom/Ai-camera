"use strict";class CaptureManager{constructor({video,canvas}={}){this.video=video;this.canvas=canvas;this.busy=false}async frame({width,height,type="image/jpeg",quality=.95}={}){if(this.busy)throw new Error("Capture busy");if(!this.video?.videoWidth||!this.video?.videoHeight)throw new Error("Camera preview is not ready");this.busy=true;try{const w=Math.max(1,Math.round(width||this.video.videoWidth)),h=Math.max(1,Math.round(height||this.video.videoHeight));const c=this.canvas||document.createElement("canvas");c.width=w;c.height=h;const ctx=c.getContext("2d",{alpha:false});ctx.drawImage(this.video,0,0,w,h);return await new Promise((resolve,reject)=>c.toBlob(b=>b?resolve(b):reject(new Error("Frame encode failed")),type,quality))}finally{this.busy=false}}actualSource(){return{width:this.video?.videoWidth||0,height:this.video?.videoHeight||0}}}window.CaptureManager=CaptureManager;